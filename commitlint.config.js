// Конвенциональные коммиты с описанием по-русски — правило в CLAUDE.md, «Коммиты».
export default {
  extends: ['@commitlint/config-conventional'],
  rules: {
    // Пресет запрещает описание с заглавной, а у нас оно — обычное предложение:
    // «feat: Отметка текущего ряда на схеме». Так его и прочтут в заметках к релизу.
    'subject-case': [0],
  },
}
