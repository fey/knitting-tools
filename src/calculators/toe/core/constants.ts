/**
 * Константы схемы мыска. Спека держит их единым источником в ядре, а не числами
 * в Tailwind-классах: геометрия значков и кадр схемы понадобятся и схеме, и плашке
 * прогресса, и расчёту прокрутки.
 *
 * Ядро — чистый TypeScript: импортов из Vue здесь нет и быть не должно.
 */

/** Сторона клетки схемы, px. Схема прокручивается внутри окна, а не вписывается в него. */
export const CELL_SIZE = 22

/** Высота окна схемы, px. На дефолтном расчёте 60 → 20 мысок помещается целиком. */
export const CHART_VIEWPORT_HEIGHT = 420
