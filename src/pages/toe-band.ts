/** Вход калькулятора ленточного мыска — подстраница `/toe-band/` (§11). */
import { createApp } from 'vue'
import '../style.css'
import ToeCalculatorPage from '../calculators/toe/components/ToeCalculatorPage.vue'

createApp(ToeCalculatorPage).mount('#app')
